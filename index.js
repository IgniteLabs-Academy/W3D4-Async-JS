// Week 3 Day 4: Async JavaScript - Callbacks, Promises, async/await

// TODO: Accept a callback and execute it
const runCallback = (cb) => {
  // call the callback function
};

// TODO: Return a promise that resolves with 'Data loaded'
const getDataPromise = () => {
  // return a Promise here
};

// TODO: Use async/await to return 'Async data'
const loadAsyncData = async () => {
  // use await and return data
};

module.exports = {
  runCallback,
  getDataPromise,
  loadAsyncData
};
